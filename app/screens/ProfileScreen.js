import React from 'react';
import { View, Text } from 'react-native';

import styles from '../config/styles';
const ProfileScreen = () => {
  return (
    <View style={[styles.galleryContainer,styles.alignCenter]}>
      <Text>Profile Screen</Text>
    </View>
  );
};

export default ProfileScreen;
